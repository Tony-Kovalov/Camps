import 'package:flutter/material.dart';

class FirstDayStations10_14Page extends StatelessWidget {
  FirstDayStations10_14Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("День 1")),
      body:  Container(),
    );
  }
}
